import React from 'react'

const MusicPlayer = ({ activeSong, isPlaying, volume, seekTime, onEnded, onTimeUpdate, onLoadedData, repeat }) => {
    return (
        <div>MusicPlayer</div>
    )
}

export default MusicPlayer